@model IEnumerable<BattleBands.Models.ApplicationUser>
<a asp-controller="Admin" asp-action="Roles" class="btn btn-sm">All roles</a>

<h2>Users</h2>
<table class="table">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>

            <td>
                <a class="btn btn-sm btn-primary" asp-action="EditUserRole" asp-route-userid="@user.Id">Access rights</a>
            </td>
        </tr>
    }
</table>