@model IEnumerable<BattleBands.Models.User>
<a asp-controller="Roles" asp-action="Index" class="btn btn-sm">Все роли</a>

<h2>Список пользователей</h2>
<table class="table">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-userid="@user.Id">Права доступа</a>
            </td>
        </tr>
    }
</table>